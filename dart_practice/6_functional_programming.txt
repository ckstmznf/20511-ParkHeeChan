# 1급 객체
 - 매개변수로 전달 가능한것,
 - 함수또한 1급 객체이다. -> 함수도 익명 함수의 형태로 인자로 넘겨줄 수 있다.


# forEach
리스트의 아이템 하나씩 넘기면서 반복을 한다.
이때 익명함수로 해당 아이템 반복에 사용할 기능을 만든다.
val items = [1,2,3,4,5];
items.forEach((e) {

})


# where
kotlin의 filter과 비슷

# map
koltin의 map과 비슷

# toList, toSet
where과 map의 결과로 나온걸 각각 list와 set으로 바꿔준다.

# any
kotlin의 any와 비슷
